sudo: required
services:
  - docker

before_install:
  - docker build -t kevingreenb1/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true kevingreenb1/docker-react npm run test -- --watchAll=false


